{
  "name": "prompt-atlas",
  "version": "0.3.0",
  "private": true,
  "description": "Visual Prompt Atlas — a linter + benchmark for visual prompts (9-slot atom dictionary)",
  "scripts": {
    "build": "node scripts/build.js",
    "build:rec": "node scripts/build-rec.js",
    "test": "node tests/run-tests.js",
    "validate:schema": "node scripts/validate-schema.js",
    "validate:core": "node scripts/validate-core.js",
    "validate:docs": "node scripts/check-docs-consistency.js",
    "validate": "npm run validate:schema && npm run validate:core && npm run validate:docs",
    "check": "node scripts/check.js"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
